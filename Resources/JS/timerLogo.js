const element = document.querySelector('.TeamLogo');

/*function sleep(ms) {
  return new Promise(resolve => setTimeout(resolve, ms));
}

async function timer() {
  while (1==1) {
      await setInterval(function(){element.style.opacity='1.0';}, 500);
      await setInterval(function(){element.style.opacity='0.7';}, 500);
  }
}

timer();8?